#!/usr/bin/env python3
"""Retired direct-file formal research entrypoint."""

MESSAGE = (
    "STOP: legacy direct-file research runner is replay-only; formal results must use "
    "a VERIFIED curated Data Layer V1 dataset ID/SHA"
)


def main(argv: list[str] | None = None) -> int:
    raise SystemExit(MESSAGE)


if __name__ == "__main__":
    main()
